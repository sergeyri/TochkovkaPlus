<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="1dp">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/dialogset_folderChooser_itemHeight"
        android:orientation="horizontal">
        <TextView android:id="@+id/folder_title"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_gravity="center_vertical"
            android:textStyle="bold"
            android:textSize="@dimen/textsize_msg"
            android:singleLine="true"
            android:ellipsize="middle"/>
        <Button android:id="@+id/folder_selector"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/dialog_inputOffset"
            android:layout_gravity="center_vertical"
            android:background="@drawable/input_bg"
            android:text="@string/dialog_actionSelectPath"/>
    </LinearLayout>
    <View style="@style/horizontal_line"
        android:background="@color/bg_actionpanelBlue"/>
</LinearLayout>